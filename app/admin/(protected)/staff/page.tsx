import { AdminStaffClient } from "@/components/admin/AdminStaffClient";

export const metadata = { title: "Staff & Roles | Fechi Organics Admin" };

export default function StaffPage() {
  return <AdminStaffClient />;
}
